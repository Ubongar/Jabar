# Actuator exposure for local development
# Inherit secure baseline (including enabled-by-default=false) and add only low-risk diagnostics.
management.endpoints.web.exposure.include=health,info,metrics
